# Treasure Hunter Web App

This is the web app for the game treasuse game.

## Commands

- `yarn start` runs application in development mode.
- `yarn test` runs unit test.
- `yarn build` generates a production build.
